# Server only. Never prefix these secrets with VITE_.
DATABASE_URL=
EDITOR_SECRET_HASH=
# Optional readable class-code hash; keeps the original key working.
EDITOR_CLASS_KEY_HASH=
EXHIBITION_ID=douglass-main
